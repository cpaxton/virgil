repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.10  # Use the latest stable version or one matching your project
    hooks:
      - id: ruff
        args: [--fix]  # Optional: enable auto-fixing
      - id: ruff-format

